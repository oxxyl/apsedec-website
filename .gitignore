# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.env
.env.production

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

APSEDEC Logo/
Partner logos/

# Source documents. The 2026 profile is small enough to track (it is the
# authority for site facts); the annual report is a 73MB .docx and is kept
# out of git — it is a reference, not a build input. Both live in docs/.
docs/*.docx
